<h1 class="text-2xl font-bold">Election results</h1>

<p class="text-gray-600">
  Election results from the past year are shown here.
  After that, data is deleted.
</p>

<% @elections.each do |e| %>
  <%= render partial: 'result', locals: { election: e } %>
<% end %>
