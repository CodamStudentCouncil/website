<div class="max-w-full mx-auto space-y-12 mt-6">
  <div class="mb-12 max-w-xl mx-auto">
    <h1 class="text-2xl font-bold">Candidates</h1>
    <p class="text-gray-800">
      There are <%= pluralize(@candidates.count, "candidate") %> in this election.
      Please read through their motivations and press the "Proceed" button below
      when you are ready to cast your vote.
    </p>
  </div>


  <div class="mx-auto max-w-6xl">
    <div class="grid grd-flow-row lg:grid-cols-2 mx-2 lg:mx-4 xl:mx-6 gap-x-8 gap-y-12">
      <% @candidates.each do |c| %>
        <%= render partial: "candidates/candidate_listing", locals: { candidate: c } %>
      <% end %>
    </div>
  </div>

  <div class="max-w-xl mx-auto space-y-6 break-inside-avoid-column mb-12">
    <p class="font-normal text-gray-800">
      On the next page, you will be able to fill out your votes for each candidate.
    </p>
    <%= link_to new_vote_url, class: "mt-4 px-4 py-2 bg-gray-500 text-white rounded" do %>
      Proceed
    <% end %>
  </div>
</div>
