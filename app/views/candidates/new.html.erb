<%= turbo_frame_tag "new_candidate" do %>

  <%= form_with model: @candidate, url: election_candidates_url(@election) do |f| %>

    <%= f.text_field :username, autocomplete: :off %>
    <%= f.submit %>

  <% end %>

<% end %>
